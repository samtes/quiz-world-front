{{outlet}}
<div class="container">
  <div class="admin">
    {{#each button in buttons}}
      {{#link-to button.page}}
        <div class="admin-button long-button"> {{button.button}} </div>
      {{/link-to}}
    {{/each}}
  </div>
</div>
